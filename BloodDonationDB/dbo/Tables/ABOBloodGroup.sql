CREATE TABLE [dbo].[ABOBloodGroup] (
    [Id]            INT          IDENTITY (1, 1) NOT NULL,
    [Name] VARCHAR (10) NOT NULL,
    CONSTRAINT [PK_ABOBloodGroup] PRIMARY KEY CLUSTERED ([Id] ASC)
);

